{
  "schema": 1,
  "description": "Determines if the user request is related to the described schema",
  "execution_settings": {
    "default": {
      "temperature": 0.0
    }
  },
  "input_variables": [
    {
      "name": "data_schema",
      "description": "Schema of the database tables",
      "required": true
    },
    {
      "name": "data_objective",
      "description": "Request from the user",
      "required": true
    }
  ]
}